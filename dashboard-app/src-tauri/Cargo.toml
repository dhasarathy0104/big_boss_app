[package]
name = "desklog-dashboard"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "blocking"] }

[[bin]]
name = "desklog-dashboard"
path = "src/main.rs"
